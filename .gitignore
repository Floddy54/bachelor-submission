# OS
.DS_Store

# Python cache
__pycache__/

# Local Python/Conda environments
.venv/
venv/
env/
.envs/
envs/
antibad24/

# Jupyter
.ipynb_checkpoints
notebooks/

# Logs
# *.log
*.out
*.err

# Results
# Commented out 2026-05-11: README and thesis §4.8 claim experiments/results/**
# ships with the repo. Keeping these patterns out so reviewers can read the
# compiled per-attack CSVs end-to-end on a plain laptop.
# experiments/results/{asr,bert,input_reduction,untargeted}/**/*.csv

# IDE
.vscode/

# Checkpoints and model weights
ckpts/
*.bin
!ANTI-BAD-CHALLENGE/classification-track/models/task1/model*/adapter_model.safetensors
*.safetensors
!ANTI-BAD-CHALLENGE/classification-track/models/task1/model*/adapter_model.safetensors
*.pth
*.pt

# LLM API keys and environment variables
.env
.secrets/
.dashboard-scratch


# Per-developer local settings (each teammate keeps their own copy).
# The committed template is configs/local.yaml.example — copy it to
# configs/local.yaml and edit the values for your own HPC account.
configs/local.yaml

# Local productivity files (not needed on HPC)
implementation_plan.md
CLAUDE.md
TASKS.md
dashboard.html
memory/
todo.txt

docs/*.txt
docs/*.json

# New layout (clean-code ML structure)
# Commented out 2026-05-11: README "About The Project" and thesis §4.8 both
# state these compiled artifacts ship with the repo so a reviewer can
# reproduce / inspect the analyses end-to-end without HPC access. Keeping
# the patterns disabled (rather than deleting) makes the intent reviewable.
# data/raw/poisoned/
# data/processed/task1/*.json
# data/processed/task1/*.txt
data/external/
# experiments/results/
# experiments/submission/

# Legacy paths (kept for safety during migration)
!ANTI-BAD-CHALLENGE/classification-track/models/task1/*/tokenizer.json
data/task1/*.json
data/task1/*.txt
textAttack/classificationTask1/*/asr/asr_cacc_results.txt

# Comma-Separated Values files that are too large
ClassificationTask1\process

# Auto-generated eval outputs
textAttack/classificationTask1/*/untargeted/untargeted_output.txt

# HF cache touches these on load
#ANTI-BAD-CHALLENGE/classification-track/models/task*/model*/tokenizer.json

# Pruned adapters are build artifacts
ANTI-BAD-CHALLENGE/classification-track/models/task*/model*_pruned_*/

# Per-teammate large artifacts (weights, raw logs) — keep only the reporting/ folder
aleksandar_data/results/
aleksandar_data/logs/
ANTI-BAD-CHALLENGE/yoel/results/


# Build artifacts — pruned + WAG-merged adapter variants
ANTI-BAD-CHALLENGE/classification-track/models/task*/model*_pruned_*/
ANTI-BAD-CHALLENGE/classification-track/models/task*/model*_wag_merged/
ANTI-BAD-CHALLENGE/classification-track/models/task*/wag_merged/

# Raw per-job result trees
# Commented out 2026-05-11 alongside the broader experiments/results/ rule
# above. The README's "Where data goes" table and the suggested-order-for-
# examiners walkthrough (steps 1, 4, 7, 8, 9) cite these subdirectories
# explicitly as bundled outputs reviewers should be able to read.
# experiments/results/asr/
# experiments/results/bert/
# experiments/results/bert_crow_defense/
# experiments/results/bert_mlm_defense/
# experiments/results/input_reduction/
# experiments/results/untargeted/

# Empty / unused result buckets
# experiments/results/bert_mlm_defense/

# Python / Jupyter caches (currently only __pycache__/ is listed)
.ipynb_checkpoints/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# OS / editor noise
Thumbs.db
*.swp
.idea/

# Aleksandar/Yoel raw output trees that aren't reporting/
ANTI-BAD-CHALLENGE/yoel/logs/
aleksandar_data/textattack_checkpoints/

_archive
